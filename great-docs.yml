# Each build generates `great-docs/_quarto.yml` from this file, so edit this file, not that one.

parser: numpy
dynamic: true

site_url: https://ozanozbeker.com/dagster-dataframely/

authors:
  - name: Ozan Ozbeker
    role: Maintainer
    email: github@ozanozbeker.com

# Adds braces to the Python fences in the build directory's copies, so `README.md` and docstrings keep plain fences for GitHub, PyPI and ruff (ADR-0009).
pre_render: scripts/brace-example-fences.py

# No `freeze:` key, so Quarto runs every page on every build, which the build check needs: a change in `src/` can break a page nobody edited. `freeze: false` would have no effect, because great-docs omits it. Keeping `_freeze/` out of git and the CI cache makes every page run again.

# A reader reads the Guide pages once, in order, and looks things up in the Reference pages. So Settings, Naming, Errors and Hand-wiring are Reference pages, not Guide pages.
user_guide:
  - section: Guide
    contents:
      - declaring-an-asset.qmd
      - the-failure-policy.qmd
      - where-invalid-rows-go.qmd
      - what-a-run-produces.qmd
      - testing-an-asset.qmd
      - partitioning.qmd
      - lazyframes.qmd
  - section: Reference
    contents:
      - settings.qmd
      - naming.qmd
      - errors.qmd
      - hand-wiring.qmd

# Grouped by module, because the default groups by kind and puts `dd.wiring` under four headings.
# The mapping form sets `title`, so the navbar shows "API" instead of a second "Reference".
reference:
  title: API
  desc: >-
    This page lists every public name with its signature, generated from the source. The
    guide explains what each one does.
  sections:
    - title: Core
      desc: >-
        `dd.asset` builds an asset from a schema, and `quarantine_spec` adds that asset's
        quarantine to the asset graph.
      contents:
        - asset
        - quarantine_spec
        - Granularity
        - SchemaRules

    - title: Hand-wiring
      desc: >-
        `dd.asset` assembles these parts. Use them to build a `@dg.asset` yourself, where
        `dd.asset` does not fit.
      contents:
        - wiring.schema_metadata
        - wiring.table_schema
        - wiring.check_specs
        - wiring.check_results
        - wiring.check_name
        - wiring.validation_results
        - wiring.AssetYield
        - wiring.QuarantineWriter
        - wiring.delegating_writer
        - wiring.file_writer
        - wiring.quarantine_frame
        - wiring.quarantine_path
        - wiring.validate_quarantine_key

    - title: Errors
      desc: >-
        This package raises these errors. Each one subclasses `DagsterDataframelyError`.
      contents:
        - errors.DagsterDataframelyError
        - errors.CollectionNotSupportedError
        - errors.ReservedColumnError
        - errors.InvalidColumnNameError
        - errors.CheckNameCollisionError
        - errors.InvalidSettingError
        - errors.MaterializeResultValueError
        - errors.MaterializeResultFieldError
        - errors.ColumnSchemaError
        - errors.ValidationAbortError
        - errors.NoValidRowsError
        - errors.QuarantineKeyCollisionError
        - errors.QuarantineDirError
