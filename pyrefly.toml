preset = "strict"
# CI's `uv sync --locked` does not install the demo, so pyrefly reads the demo's modules from
# `demo/src`. CI then type-checks the demo without installing its webserver.
search-path = [".", "demo/src"]
